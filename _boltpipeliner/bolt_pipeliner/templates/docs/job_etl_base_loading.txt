import sys
import logging
import types

from pyspark.sql import functions as F  # noqa: F401
from bolt_pipeliner.sessions import create_session

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

# Pick session profile from the BOLT_SPARK_PROFILE env var (default: local).
import os as _os
spark = create_session(_os.environ.get("BOLT_SPARK_PROFILE", "local"))

logger.info("=" * 60)
logger.info("Loading ETLBase")
logger.info("=" * 60)

{etl_base_code}
